# ===========================================
# SERVER-ONLY CONFIGURATION
# These variables are only used on the server
# ===========================================

# WebSocket Server Configuration
# Host to bind the WebSocket server to
# Use 0.0.0.0 to allow remote connections, or 127.0.0.1 for localhost only
WS_HOST=0.0.0.0

# Port for the WebSocket server
WS_PORT=8080

# Node Environment
NODE_ENV=production

# ===========================================
# CLIENT CONFIGURATION (OPTIONAL)
# Clients will be prompted to enter server details at startup
# These are fallback values if you want to pre-configure
# ===========================================

# WebSocket Server URL for client connections (optional fallback)
# Leave empty to always prompt user at startup
# When specified: ws://YOUR_SERVER_IP:8080 or wss://your-domain.com
WS_SERVER_URL=

# Note: Client applications will prompt for:
# - Protocol (ws:// or wss://)
# - Host (IP address or domain name)
# - Port (default 8080)
# This provides flexibility for users without modifying code
